# Photon Spectrum credentials (app.photon.codes → project settings)
PROJECT_ID=
PROJECT_SECRET=

# The dream backend this daemon serves (localhost when co-located on the VM)
BACKEND_URL=http://localhost:3001

# Seconds between polls of the outbox + link-request queues
POLL_INTERVAL_SEC=20
